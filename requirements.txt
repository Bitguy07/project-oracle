# Project Oracle — Python Dependencies
# Install: pip install -r requirements.txt

# Google GenAI SDK (current, supported)
google-genai>=0.7.0  # Latest stable; or pin if needed (e.g., ==0.7.1)

# HTTP client (async)
httpx>=0.27.0

# Standard utilities
python-dotenv>=1.0.0      # Local .env file support

# Type hints / data validation
typing-extensions>=4.10.0